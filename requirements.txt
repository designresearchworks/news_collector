# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Jinja2 templates (FastAPI's templating layer)
jinja2>=3.1.3

# OpenAI-compatible client (used to call OpenRouter)
openai>=1.30.0

# Settings management (reads from .env file)
pydantic-settings>=2.2.0

# For python-multipart (needed by FastAPI for form data, good to have)
python-multipart>=0.0.9
